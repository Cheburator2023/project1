apiVersion: v1
kind: Service
metadata:
  name: {{ include "mrms-backend.fullname" . }}
  labels: {{- include "mrms-backend.labels" . | nindent 4 }}
spec:
  ports:
    - name: 3000-tcp
      protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: {{ include "mrms-backend.fullname" . }}
